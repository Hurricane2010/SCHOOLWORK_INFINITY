function standCalc() {
    var div = document.getElementById("sciCalc");
    div.style.display = div.style.display == "none" ? "block" : "none";
}

function graphCalc() {
    var div = document.getElementById("graphDesmos");
    div.style.display = div.style.display == "none" ? "block" : "none";
}

function geoCalc() {
    var div = document.getElementById("geoCalc");
    div.style.display = div.style.display == "none" ? "block" : "none";
}